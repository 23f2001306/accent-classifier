__pycache__/
*.py[cod]
.venv/
venv/
.deps/
.streamlit/secrets.toml
*.wav

# Keep deployment model files in the repository so Streamlit Community Cloud
# can load them. Use Git LFS instead if the model becomes large.
